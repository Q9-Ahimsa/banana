{
  "name": "banana",
  "version": "0.1.0",
  "description": "Harness-neutral continuity kit: installer, project init, per-intent briefs, liveness doctor",
  "type": "module",
  "bin": {
    "banana": "bin/banana.mjs"
  },
  "scripts": {
    "check": "tsc --checkJs --noEmit",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^5.5.0"
  }
}
